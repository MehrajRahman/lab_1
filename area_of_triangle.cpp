#include <stdio.h>
int main()
{

    int height, base;
    double area;
    scanf("%d %d", &height, &base);
    area = .5 * height * base;
    printf("Area: %.2lf", area);
    return 0;

}
